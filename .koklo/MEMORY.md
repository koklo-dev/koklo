# Project Memory

<!-- Hand-curated long-term memory injected into every agent prompt.
     Keep this concise — agents read it on every pipeline run.
     Add decisions, constraints, and context that isn't obvious from the code. -->
