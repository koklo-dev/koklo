{
  "name": "@koklo/trpc-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo '[@koklo/trpc-client] no-op: package is a P2 placeholder, nothing built (see sprints/backlog)'",
    "lint": "echo '[@koklo/trpc-client] no-op: no linter configured, deferred to P2 (see sprints/backlog)'",
    "typecheck": "echo '[@koklo/trpc-client] no-op: no sources to typecheck yet, deferred to P2 (see sprints/backlog)'"
  }
}
