# Sprint 03 — Agent Runtime + Pipeline Orchestrator

## Objectif
`koklo run feature "test"` lance le pipeline avec gates humaines.

## Statut
Implémenté (sans TUI pour l'instant)

## Tâches
- [x] `crates/agent-runtime` — AgentRunner
- [x] `crates/workflow-engine` — PipelineOrchestrator + 5 phases + gates

## Critère de validation
Phase PM génère docs/planning_artifacts/spec.md
